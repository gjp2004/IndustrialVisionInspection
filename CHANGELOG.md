# 变更记录

## 1.1.0 - 2026-07-23

### 新增

- 独立`CIRCULARITY_NG`圆度判定。
- `ASPECT_RATIO_NG`长宽比判定。
- `POSITION_NG`中心位置偏移判定。
- 长宽比和中心偏移的SQLite、CSV追溯。
- 旧版数据库字段自动迁移。
- PLC周期号去重，防止相同START重复检测。
- 应用级异常日志和友好错误提示。
- 深色/亮色目标切换和自适应阈值。
- 可选像素当量标定，输出毫米宽高和平方毫米面积。
- 标定测量写入SQLite、历史表格和CSV。
- 检测记录新增产品型号、操作员、配方名称/版本和PLC周期号。
- 历史记录支持按产品型号筛选。
- 自动模式锁定检测参数和配方信息，降低运行中误操作风险。
- PLC周期号去重跨断线重连仍然生效。
- 新增推送前大文件、密钥格式和测试审计脚本。
- 发布脚本自动读取项目版本，避免版本目录不一致。
- 历史统计改为使用当前筛选结果，并显示NG代码分布。
- 增加结果、批次、产品和日期组合查询测试。
- 增加确认式NG孤立图片清理，仅处理超过7天且数据库未引用的托管PNG。
- 清理过程跳过管理目录外路径、无效引用和重解析目录。
- 增加系统自检，覆盖数据目录写入、SQLite、OpenCV和默认配方。
- 最终EXE支持`--self-test`无界面诊断并通过退出码报告结果。
- 本地预推送检查和GitHub Actions均执行发布包级冒烟测试。
- 增加1000次视觉处理与释放的托管/私有内存增长回归门槛。
- 提取`IPlcClient`接口，主业务和心跳不再依赖具体文本TCP实现。
- 提取`ICameraFactory`，主业务不再直接创建模拟或USB相机。
- 增加每日JSONL操作审计，覆盖检测、配方、PLC周期和NG清理。
- 审计写入线程安全，失败与业务主流程隔离。
- 配方内容变化时覆盖前自动备份，相同内容不产生重复备份。
- 每个配方只保留最近20个历史版本，并清理临时文件。
- 增加版本标签触发的GitHub Release自动发布工作流。
- 增加Dependabot、Bug/功能Issue表单和本地GitHub配置检查。
- GitHub Actions自动构建、测试和Windows发布产物。
- 优化主窗口工具栏间距、控件尺寸和参数区可读性。
- 增加自动生成的主窗口PNG截图，并用于README项目展示。

### 测试

- 自动化测试由24项增加到58项。
- 增加圆度、长宽比、中心偏移、数据库迁移和PLC重复周期测试。
- WPF界面测试会加载示例、执行检测、渲染PNG，并校验截图尺寸和文件完整性。
- Release工作流在`--no-restore`发布前显式还原`win-x64`资产，保证干净Runner可复现。

## 1.0.0

- 完成图像采集、视觉检测、配方、历史记录、NG图片和PLC模拟通信闭环。
