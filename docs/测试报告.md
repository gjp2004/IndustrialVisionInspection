# 测试报告

## 测试环境

- Windows x64
- .NET SDK 8.0.423（隔离安装）
- Release 配置
- OpenCvSharp 4.10
- Microsoft.Data.Sqlite 8.0

## 自动化测试范围

测试工程：`tests/IndustrialVisionStudent.Tests`

- 单目标视觉测量与OK判定
- 内置示例图与默认参数端到端判定
- 多目标数量NG
- ROI排除目标与ROI越界
- 非法参数验证
- 宽度规格NG
- 亮目标/暗背景检测
- 自适应阈值检测与窗口参数验证
- 像素当量换算和非法标定值验证
- 独立圆度NG
- 长宽比规格NG
- 中心位置偏移NG
- JSON配方往返保存
- 配方相同内容去重、覆盖前备份恢复和最近20版保留
- SQLite保存、结果/批次查询和统计
- 旧版SQLite数据库自动迁移
- 产品、操作员、配方版本、PLC周期追溯与产品筛选
- 结果、批次、产品和日期组合筛选
- 当前筛选统计、NG代码分布和空结果统计
- NG证据图引用去重查询
- 旧孤立NG图片安全清理、引用保留和目录边界
- 系统自检全部通过和默认配方缺失的可读失败报告
- Windows x64最终EXE的`--self-test`发布包级冒烟验证
- CSV中文BOM及特殊字符转义
- WPF主窗口STA线程初始化、布局和关闭
- 假相机启动、取帧、停止和重复启动
- USB摄像头无效设备编号的安全失败
- 内置模拟摄像头连续帧采集
- 默认相机工厂来源、模拟/USB创建、非法编号和未知类型
- JSONL审计格式、100次并发写入、空操作拒绝和写入失败隔离
- WPF窗口接收模拟摄像头实时帧并安全停止
- 示例图检测后自动生成SQLite历史记录
- 相机采集失败后的重连恢复
- TCP PING与START/BUSY/RESULT完整握手
- 重复PLC周期号去重保护
- PLC断线重连后的重复周期号去重
- 心跳监控通过可替换PLC客户端接口运行
- 自动模式参数锁定
- PLC心跳
- 500次视觉循环
- 100次预热后连续1000次视觉处理与释放的资源增长回归
- 200次SQLite写入
- 50次连续TCP请求

## 最近一次结果

```text
通过：58
失败：0
跳过：0
Release编译：0警告、0错误
WPF界面级测试：通过（真实创建窗口、加载示例、执行检测、渲染PNG并关闭）
截图完整性：通过（1280×800，文件大于10 KB）
```

## 30分钟混合长稳结果

完整报告：`artifacts/stability-report.json`

```text
请求时长：1800秒
实际时长：1800.162秒
视觉循环：57,603次
PING请求：2,880次
PLC完整检测握手：576次
SQLite写入：1,152条
结束时数据库记录：1,152条
平均视觉处理耗时：约0.960 ms/次
峰值工作集：80,605,184字节（约76.9 MB）
错误：0
```

工作集在前4分钟上升到约76.7 MB，此后至30分钟结束基本稳定在76.7～76.9 MB。

短时CI资源回归门槛：

```text
托管内存保留增长：小于8 MB
进程私有内存增长：小于32 MB
```

## 尚需实物验证

自动化测试无法替代真实摄像头和实际光照环境。USB摄像头的设备编号、分辨率支持、曝光和现场图像效果需要在目标电脑上确认。PLC通信当前通过本机TCP模拟器验证，不代表真实PLC网络环境。
